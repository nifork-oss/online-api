name = "online-api"
main = "worker.js"
compatibility_date = "2025-01-01"

# Секреты (JSONBIN_KEY, SESSION_SECRET, SEED_ADMIN_EMAIL, SEED_ADMIN_PASSWORD)
# сюда НЕ вписываются — они задаются отдельно через:
#   Cloudflare Dashboard -> Worker -> Settings -> Variables and Secrets
# или командой: wrangler secret put ИМЯ_СЕКРЕТА
